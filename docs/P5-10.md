# Su Scheduler — P5-10 执行记录：设备矩阵扩展与发布验证

> **任务**：P5-10 · 扩展真实 Android 验证范围
> **前置**：P5-01（1/15 矩阵基线）；P5-05（新 Trigger 真机）；D-P5-01..05（稳定性修复）
> **日期**：2026-09-07
> **性质**：单机发布验证（本环境唯一真机 = KernelSU × Android 16）——在完整当前构建
> （Runtime 1.30.0）上验证 P5 全部新功能 + 发布流程；其余 14 格如实标记发布限制。
> **范围**：不新增 IPC op；不改 Legacy 路径；设备矩阵如实记录（不伪造通过）。

---

## 1. 交付物与状态

| 交付物 | 状态 |
| :--- | :--- |
| docs/P5-10.md（本文档） | ✅ |
| 设备矩阵更新（P5-BASELINE-COMPATIBILITY.md §4 回填） | ✅ |
| docs/P5-CANDIDATES.md：P5-10 观察项登记（4 条稳定性/可用性） | ✅ |

---

## 2. 设备环境

- **设备**：Xiaomi 17 (`pudding` / `25113PN0EC`)，adb `adb-8934ffc4-...`，KernelSU（root，SELinux Enforcing），Android 16 (API 36)。
- **模块**：`su-scheduler-v1.6.8.zip`（WSL `bash build.sh`，152,045 B）→ `ksud module install` → reboot 激活 → `/system/bin/su-scheduler{,-d,-termux,-runtime}` 就位。
- **Runtime**：`1.30.0`（daemon log `Runtime library loaded (v1.30.0)` 确认，覆盖旧 v1.28.0）。

---

## 3. 发布验证结果（A–H 必测场景）

| 场景 | 结果 | 证据 |
| :-- | :-- | :-- |
| **A. 全量回归** | ✅ | host 48 套件 2236 PASS/0 FAIL；p3-device 28/0；p1-device 11/0 |
| **B. 新 Trigger** | ✅ | oneshot 到点自删；interval:1 周期 6×；delay:1 一次；cron 命中+同分钟去重（50+ tick 仅 1 exec）；boot_completed 一次；schedule_state 键 `iv_/dl_/dlx_/bc_/cn_` 齐备；audit `mode=managed` |
| **C. Condition 运算符** | ✅ | `time.hour < 8` 执行；`time.hour > 20` → `op=cond\|blocked=1` 无 run dir；`env.CONFIG_FILE contains su-scheduler` 执行；EDIT_TASK 保存 roundtrip 一致 + 调度求值生效；越界 `time.hour < 24` 正确拒绝 |
| **D. WAITING 和 Retry** | ✅ | 依赖未满足 → gate_wait→WAITING→keep-waiting→(依赖完成)→gate_ok→执行；失败 exit 1 → FAILED→rearm WAITING(60s backoff)→attempt 1/2→2/2 |
| **E. WebUI/批量控制** | ✅ | GET_SUMMARY 含 `counts.recovering`/task `dependency`/`dep_errors`；GET_TASK_DETAIL 含 `condition_state`/`dependency_state`；批量 `task start/stop` 逐任务返回 |
| **F. daemon 重启恢复** | ✅ | RUNNING 残留 → restart → `daemon_restart\|FAILED\|residual` → FAILED；daemon 自愈 Alive |
| **G. Legacy 回归** | ✅ | p1-device 干净启动轮全绿（config.txt legacy 零改动） |
| **H. 升级/回滚** | ✅ | 升级：v1.28.0→v1.30.0 覆盖安装；回滚：卸载+重装当前 zip，数据保险库保留（mode/task/audit/schedule_state 全保留） |

---

## 4. 设备矩阵更新

| 管理器 | Android 12 | 13 | 14 | 15 | 16 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| KernelSU | ⏳ | ⏳ | ⏳ | ⏳ | ✅ `adb-8934ffc4-` |
| Magisk | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| APatch | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |

- **已覆盖**：KernelSU × Android 16（本环境唯一真机）——P5 全部新功能 + 发布流程验证通过。
- **未覆盖 14 格（发布限制，不伪造通过）**：KernelSU×A12/A13/A14/A15、Magisk×A12–A16、
  APatch×A12–A16——本环境无对应设备/模拟器。P5 出口报告须如实标注。

---

## 5. 稳定性/可用性观察（P5-10 发现，非阻断，登记 P5-CANDIDATES）

| ID | 现象 | 判定 | 建议 |
| :-- | :-- | :-- | :-- |
| O-1 | p1-device 5 连发 `restart` 与 service.sh watchdog 竞态，crash_seq 0↔1 翻转，偶发累积→300s 降级→smoke FAIL（4 轮中 3 轮受扰） | 已知 crash-guard×watchdog 竞态类型，非 P5 功能缺陷；D-P5-05 已在 p3-device 用「restart 前清 guard」规避 | 给 p1-device 做同款 guard 时序处理（测试基建），或专项排查 restart×watchdog |
| O-2 | 主循环单次迭代偶发 >60s（实测 71s 间隙跳过 00:51），精确 HHMM 任务无 catch-up 补跑 | 罕见；与 restart 风暴/降级窗口叠加；非 P5 功能缺陷 | 评估 catch-up 机制（P6 候选） |
| O-3 | `task-config new` 无法创建 cron 触发（trigger 含空格 → `tcfg_new_id` 派生 ID 失败，明确报错无破坏）；cron 经 task 文件/`apply` 路径正常 | CLI 可用性；功能路径不受阻 | 优化 `tcfg_new_id` 对含空格 trigger 的 ID 派生（后续任务） |
| O-4 | EDIT_TASK `configuration_invalid` 吞掉具体 stderr；`su-scheduler ipc` 对已编码 payload 双编码被拒 | 可观测性/易用性；行为正确（拒绝） | 后续增强错误透传 |

---

## 6. 验收对照

| 验收命令 | 结果 |
| :-- | :-- |
| `bash tests/run_tests.sh --with-device` | host 48 套件 2236/0 + p1-device 11/0 + p3-device 28/0 |
| `bash tests/p3-device/smoke.sh --with-device` | 28 PASS / 0 FAIL / 0 BLOCKED |
| `bash tests/p5-trigger/test.sh` | 137 PASS / 0 FAIL |
| `bash tests/p5-condition/test.sh` | 60 PASS / 0 FAIL |

期望满足：每个实际覆盖平台单独记录（KernelSU×A16）；未覆盖平台（14 格）如实标记为发布限制。

---

## 7. 约束自查

| 约束 | 核查 | 结果 |
| :-- | :-- | :-- |
| C1 最小 diff | 本任务生产零改动（仅 docs + 设备验证） | ✅ |
| C2 不删旧路径 | Legacy 路径实测零改动（p1-device 全绿） | ✅ |
| C3 无新增依赖 | 无 | ✅ |
| C4 config.txt 格式 | 未触碰 | ✅ |
| C5 不提前实现 | 无 DAG/云/多设备；P6 候选仅登记 | ✅ |
