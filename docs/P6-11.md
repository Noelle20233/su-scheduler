# Su Scheduler — P6-11：综合性能、安全与发布验证（宿主部分）

> **任务**：P6-11 · P6 阶段综合验证收口（宿主侧完整取证 + 综合报告；设备离线，
> 真机复验与 `--with-device` 组合延后——延后清单见 §7，含自 P6-10 遗留的补全）。
> **基线**：HEAD `be1135b`（P6-01..10 全部提交；Runtime **1.32.0** / module **v1.6.8**，
> 八处版本一致由 `tests/p1-build/build_check.sh` 守卫）。宿主基线：run_tests 50 套件全绿。
> **纪律**：**零生产改动**（`git diff` 生产面 0 行）；既有断言**零改**（`tests/run_tests.sh`
> 仅 +4 行：注册 `tests/p6-verify/test.sh` + 头注释 3 行）；净新增回归资产
> `tests/p6-verify/test.sh`（**30 断言**，已注册进 run_tests 并全绿）；验证暴露的产品
> 观察**只登记**（§5，O-P6-11-01）；未 commit/push。
> **环境**：全量在 WSL 原生 FS `~/su-scheduler`（AGENTS §4.4）；跑数前 load 检查
> （起测 `0.00 0.00 0.00`）；§perf 近界档采用 P6-09 A/B 地板口径防高负载误红。

---

## 0. 交付与结论摘要

| 交付 | 状态 |
| :-- | :-- |
| `tests/p6-verify/test.sh`（净新增 30 断言：tick 三档 + IPC 延迟基线 + 并发拒新时延 + shell 兼容扩展面 + I/O 级原子性演示 + 双模式互切实弹 + P5↔1.32 旧→新→旧→新宿主演练） | ✅ 30/0（最终版独立 3 连跑稳定 + 聚合内绿；全版本累计 7 次独立全绿） |
| `tests/run_tests.sh` 注册（+1 套件行、+3 注释行，零删改既有内容） | ✅ **51 套件 ALL SUITES GREEN** |
| 九检查项宿主逐条取证（§1）+ 性能基线表（§2）+ 注入面计数（§3）+ B9 面（§4） | ✅ |
| 硬门禁 10 条全量重跑（§6） | ✅ 0 FAIL |
| 缺陷登记 O-P6-11-01（§5，仅登记不修）；延后清单补全（§7）；遗留风险（§8） | ✅ |

**总裁决**：P6 宿主面综合验证**通过**——无病态性能回归（tick/IPC/并发/压力三点对比全在
带内）、注入防护面 93+ 断言全绿、构建/版本/发布一致性 29/0、双模式与升降级数据面兼容实证。
设备面证据按 §7 延后（P6-10 §3 清单并入并补全）。

---

## 1. 九检查项逐条结论（数字 + 证据套件）

| # | 检查项 | 结论 | 关键数字 | 证据（套件·断言） |
| :-- | :-- | :-- | :-- | :-- |
| 1 | **主循环耗时与 IPC 响应延迟** | **达标·无回归**（三档 tick + IPC 延迟基线首次成文） | tick：空 **50ms** / 50 任务 **1695ms** / 32 节点链满额稳态 **1682–1741ms**（界内，§2 趋势）；IPC：GET_TASKS 空载 max **94–106ms**（≤1000）、VALIDATE max **157–172ms**（≤1000）、50 任务数据面 max **963–1164ms**（≤2500 记录性）、`ipc_client_send` 真往返 **rc=0**（墙钟 ~1030ms=两端 1s 轮询粒度设计语义，生产 daemon 亦 1s poll 面，L1 `su-schedulerd` L899-905） | `tests/p6-verify/test.sh` §perf3（3）+ §ipc（4）；复核 `tests/p6-reliability/test.sh` §perf（25/0，50-task=1699ms） |
| 2 | **大量任务、深链、并发链压力** | **达标**（resource 全套复跑绿 + 拒新时延补断言） | 100 任务注册单循环/上限 26 断言全绿；H1 32 节点 pass 峰值 **1248ms**、H2 深 16 **606ms**、H5 400 历史×8 链 **394ms**、H5c 稳态 **355ms**、H6 320+ run.txt **784ms**（≤2000）；**新增**：9 链并发拒新登记波 pass **1724–1734ms**（≤3000 一次性首扫界，O-P7-04 同源）、稳态 pass **489–492ms**、在途 8 run 全 RUNNING + 8 节点恰 1 次派发零误杀 | `tests/resource/stress.sh`（26/0 复跑）+ `tests/p6-verify/test.sh` §conc（conc-1/2/3，净新增时延断言——P6-07 H3 仅状态面无时延界） |
| 3 | **shell 兼容** | **达标**（真机 mksh 复验延后 §7） | `dash -n` 全部 6 生产脚本通过（L1 为 `sh -n`；显式 dash 面净新增）；mksh 静态断言（D36/D37：`${}` 零裸 `\|`/`(`/`)` in-pattern）RTLIB 既有 + **CLI/daemon 入扫描面**（0 命中，含 P6-02/03/04/06/07/10 全部新增代码）；`bash -n`：p6-device/p6-reliability/resource/verify-self；**`node --check webroot/app.js` 通过**（P6-08 前端资产，工具在场断言/缺席 NOTE）；**dash 真跑探针**（source runtime → tick → ipc_server_poll → `scheduler_dag_pass` 全链路 rc=0，P5-09 先例「静态断言+dash 真跑+真机终裁」）；L1 覆盖确认：p6-cli/p6-webui/p6-dag 各含自检 §posix（47/82/177 套件内绿） | `tests/p6-verify/test.sh` §sh（6 断言）+ `tests/lint/syntax.sh`（11/0）+ 各套件 §posix |
| 4 | **依赖/Condition/Trigger/DAG 注入防护** | **达标**（复跑零回退） | 汇总**注入/防护直接断言 93**：`security/fuzz` **37**（含链注入面 9：8 dep 注入+超长/空格 id、CREATE 孤儿/穿越、EDIT 注入边、伪造 dag 树免疫；副作用文件恒不存在）+ `ipc/security` **17**（11 malformed/injection→invalid_request、零 Root 动作、config 逐字节不变）+ `security/path-validation` **9** + `p6-dag §inject` **30**（IJ-01/02 strings.tsv 逐条真跑 secv_id_ok/dep_validate、IJ-03 5 注入 Task 文件拒、IJ-04 双层图门、IJ-05 /tmp/pwn 零求值、IJ-06 全表零误判）；`p6-dag` 全套 **177/0** 复跑（含 §P7 12：token 门/伪造目录/run.txt 注入/直调非法零副作用） | `tests/security/fuzz.sh`、`tests/ipc/security.sh`、`tests/security/path-validation.sh`、`tests/p6-dag/test.sh` §inject/§P7 |
| 5 | **配置原子写与回滚** | **达标 + I/O 级真实演示** | B9/逐字节不变断言面（grep 计数）：`p4-dependency` B9×6/逐字节×8、`p5-trigger` B9×2/×1、`p6-dag` B9×1（EX-19b import 拒→config 逐字节）/×4（EX-19c dag 闲置）、`webui/editor` B9×3（保存失败旧配置 md5 不变+无 tmp）、`webui/security`×1、`config-v2` 逐字节×6 + validation §atomic（非法 payload rc=1 不写盘/旧配置不变/tmp 清理/id 一致）、`security/fuzz`×2、`p6-cli` apply 拒绝零副作用；**真跑演示（净新增）**：`.tmp.$$` 路径被占制造真实写失败 → `tcfg_apply_task` **rc=2**（与校验拒绝 rc=1 可区分）、目标文件 `cmp` 逐字节不变、无半成品 tmp 残留 | `tests/p6-verify/test.sh` §atomic（3）+ 上列既有套件复跑全绿 |
| 6 | **安装包完整性与版本一致** | **达标** | `tests/p1-build/build_check.sh` **29/0 SKIP=0**：`build.sh` 产物 zip、`unzip -t` 完整性、zip 含 `module.prop/service.sh/customize.sh/system//webroot/`、**八处版本一致**（module.prop version/versionCode、build.sh VERSION、两 bin VERSION、README badge、update.json version/versionCode+downloadURL、zip 内 runtime `1.32.0`）、`update.json` 合法 JSON、release.yml YAML 合法、**再生幂等**（二次构建 docs==pre-build snapshot，O-P6-10-04 收口）、zip docs 含 `chain` 命令行×2 | §6 门禁复跑；run_tests 聚合内同绿 |
| 7 | **Legacy/Managed 双模式** | **达标 + 互切实弹（净新增）** | 指认既有：`legacy/golden` **8/0**、`legacy/delete-pipeline` 4/0、`p1-regression` **44/0**、`shadow` **23/0**、`idmap` **25/0**、`cli` 29/0（managed 全套=config-v2 44/41、scheduler-prod 33/0、ipc 64/0、task-control 60/0、webui×3 105/0、p4 220/0、p5 系列 334/0、p6 系列 336/0 全绿）；**双模式互切不炸**：指认 `config-v2/test.sh` §6（写入中断无半成品）/§7（`tcfg_rollback`→MANAGED 移除→mode=legacy、文件惰性保留）+ `DAG-EX-19c`（非 managed 时 `$base/dag` 逐字节闲置）；**并补实弹**（p6-verify §dual）：8 活跃 run + pending 的 managed 树 → 切 legacy tick → **dag 树 sha256 聚合逐字节不变、零新增派发** → 回切 managed → 引擎续跑（run 收敛 SUCCESS、节点零重派）= 3 断言 | `tests/p6-verify/test.sh` §dual(3) + `tests/legacy/golden.sh`、`tests/p1-regression/test.sh`、`tests/shadow/test.sh`、`tests/idmap/test.sh`、`tests/config-v2/*`、`tests/p6-dag/test.sh` EX-19 |
| 8 | **升级后状态恢复（宿主侧）** | **达标——净新增「旧→新→旧→新」跨二进制演练**（设备版=P6-10 §9 真机已证；本组为其宿主回归资产，与既有 recovery(25/0)/P6-02-5(同二进制) 不重复：以 **git 历史构建的真·P5 Runtime 1.30.0** 跑数据面） | `upg-0`：旧库自 tag `p5-baseline-v1.6.8-runtime1.30.0`（回退 sha `cbd065f`）提取，`RUNTIME_LIB_VERSION=1.30.0`、dag/last_tick 代码面 **0 命中**；`upg-1`：新版建链（root@0830→登记→派发，run RUNNING、leaf 在途恰 1）；`upg-2`：**旧版 tick 在新数据面 rc=0、`$base/dag` 逐字节惰性**（未知目录不触碰）；`upg-3`：旧版继续调度时间任务 ut2@0833 恰 1 次、chain 族零派发；`upg-4`：**run.txt 账本新版续读**（同 run token 收敛 SUCCESS、零重派——回滚窗口无损）；`upg-5`：**LOCK**＝同日回滚期旧版已执行窗口被新版 catch-up 重放（ut2 总执行=2，登记 §5 O-P6-11-01）；`upg-6`：**旧版 IPC 控制面**在新数据面 GET_TASKS rc=0 且 dag 树仍逐字节不变（回滚可运行性·控制面）；`upg-7`：**cycle/last_tick 缺失**的旧版产物 base → 新版首拍不误补 0830 窗口、last_tick 正确接管（跨二进制版 P6-02-5）。CI `fetch-depth=1` 无历史时本组整组 **[SKIP]**（不计失败，已实现） | `tests/p6-verify/test.sh` §upgrade（upg-0..7，8 断言）+ 指认 `tests/recovery/test.sh` 25/0、`tests/p6-dag/test.sh` EX-19/EX-20e、`docs/P6-10.md` 场景 9（真机版） |
| 9 | **回滚后旧版本可运行（宿主侧）** | **达标**（指认既有 + 最小补测已并入第 8 项演练） | 指认：`DAG-EX-19`（legacy 词表零 chain）/`EX-19b`（chain-token 行 import 拒且 config 逐字节）/`EX-19c`（非 managed 引擎闲置）+ `legacy/golden` 8/0 + `legacy-adapter` 107/0（旧解析语义恒可用）；**最小补测**（缺）：旧版**数据面** tick（upg-2/3）与旧版**控制面** ipc（upg-6）在含 dag 树/run.txt/managed 配置的新版数据上运行 rc=0、零触碰、零崩溃——即「回滚到 1.30 后旧版可运行」的宿主实证 | `tests/p6-verify/test.sh` §upgrade + `tests/p6-dag/test.sh` §EX-19（3）+ `tests/legacy*/` |

---

## 2. 性能基线表（三点趋势：P6-01 → P6-07 → P6-11，WSL 原生 FS 宿主 ≈0.2–0.5ms/fork）

### 2.1 scheduler_tick 三档（p6-verify §perf3，P6-11 首测第 3 档；每档预热后计时）

| 档 | P6-01（1.30 基线） | P6-07（1.31 引擎后） | **P6-11（1.32.0）** | 界 | 判定 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 空 registry | 44ms | 50ms | **48–60ms**（聚合内 50） | ≤200ms 记录 | 无退化 |
| 50 任务（纯决策） | 1731ms | 1672ms（无回退） | **1614–1697ms**（复核 p6-reliability 1699） | ≤2500ms 宽松 | **无退化**（带内噪声域同 P6-01 水平） |
| 32 节点链满额稳态（4 在途=DAG_PARALLEL_MAX 闸 + 27 defer） | —（无引擎） | H1 **pass** 峰值 1223ms（pass 口径） | 整 tick **1682–1741ms**；登记+首派波 pass **1724–1734ms**（一次性 O(roots) 首扫，O-P7-04 同源） | A/B 地板（=full50×2.2+800ms）+≤3000 绝对界；首扫波 ≤3000 | 无病态回归（P6-09 A/B 先例固化为测试内建规则，高负载两档同涨不误红） |

### 2.2 IPC 端到端响应延迟（**首次成文基线**，入队(tmp+mv)→响应可见，10ms 等待粒度）

| 请求 | 5 次 max | 界 | 说明 |
| :-- | :-- | :-- | :-- |
| GET_TASKS（空 registry） | **94–106ms** | ≤1000ms | 纯通道响应性（≈轮询唤醒 50ms 粒度 + 处理） |
| VALIDATE_TASK（合法 trigger+command） | **157–172ms** | ≤1000ms | 含字段校验+沙箱 task 文件写 |
| GET_TASKS（50 任务数据面） | **963–1164ms** | ≤2500ms | payload 聚合 O(n)（每任务多子进程读取）——**记录性基线**，病态回归判据 |
| `ipc_client_send` 真往返 | **rc=0**，墙钟 ~1029–1040ms | 仅断言 rc | 客户端 1s 轮询 + **生产 daemon 亦 1s poll**（`su-schedulerd` L899-905）→ E2E ≈1–2s 为**设计语义**（测「无病态回归」非极致优化，任务书口径） |

### 2.3 资源/压力复跑对比（`tests/resource/stress.sh` 26/0 全绿）

| 场景 | P6-07 修后 | **P6-11 复跑** | Δ | 判定 |
| :-- | :-- | :-- | :-- | :-- |
| H1 32 节点 pass 峰值 | 1223ms | **1248ms** | +2% | 带内 |
| H2 深 16 pass 峰值 | 591ms | **606ms** | +3% | 带内 |
| H5 400 历史×8 链 | 390ms | **394ms** | +1% | 带内 |
| H5c 高水位稳态 | 344ms | **355ms** | +3% | 带内 |
| H6 40 链×8 run.txt | 632ms | **784ms** | **+24%** | 如实注记：P6-10 §27 年龄钳制（`dag_token_age_le` 每登记候选一次 awk 历法差）所致常数开销；仍 ≤2000ms 界内、无阶乘项——**非回归**，趋势入表 |
| IPC 频率限制（stress-d） | 10 ok+5 rl | **10 ok+5 rl**（rc7 生效） | = | 达标 |
| 主循环恰 1 / 库零常驻 | 1/0 | **1/0** | = | 达标（B20） |

---

## 3. 注入防护面计数（§4 项汇总）

| 套件 | 断言数 | 注入面要点 |
| :-- | :-- | :-- |
| `tests/security/fuzz.sh` | **37/0**（其中链侧 9） | 8 链 dep 注入+超长/空格 id 全拒（rc∈{1,4}）、零执行、副作用文件不存在、CREATE 孤儿/穿越（穿越 id 任何逃逸位点零落盘）、EDIT 注入边不求值、伪造 dag 树免疫（corrupt→FAILED 有界、rc 恒 0） |
| `tests/ipc/security.sh` | **17/0** | 11 malformed/injection→invalid_request、零 Root 动作、task-config 逐字节、START 只执行存储命令、未知参数键拒 |
| `tests/security/path-validation.sh` | **9/0** | 穿越/符号链接/允许目录约束/id 门/IPC 穿越零泄露 |
| `tests/p6-dag/test.sh` §inject | **30/0** | strings.tsv 逐条 secv_id_ok+dep_validate 真跑（含对照）、注入 Task 文件×5 拒、双层图门、/tmp/pwn 零求值、全表零误判 |
| `tests/p6-dag/test.sh` §P7（安全相关） | 12（含于 177） | token 门 12 位字面枚举、伪造 run 目录回收、run.txt 注入纯数据、注册直调零副作用、幂等/去重 |
| `tests/p6-verify/test.sh` §upgrade（面重叠注记） | 8 | 旧版引擎对新数据面零触碰（升级/回滚路径无隐式求值面） |
| **合计注入/防护直接断言** | **93**（37+17+9+30）+ P7 12 | **全部 0 FAIL**（fuzz 内 config 逐字节不变断言 ×2、ipc 内 ×1 与 §4 计数有交叠，不重复计） |

---

## 4. 配置原子写与回滚（B9）面汇总

- **校验拒绝路径（既有，指认）**：`config-v2/validation.sh` §atomic（非法不写盘/旧配置不变/tmp 清理/payload id 一致）；`webui/editor.test.sh` §atomic B9×3（保存失败 md5+无 tmp）；`p4-dependency` B9×6+逐字节×8（editor/store/CLI/图校验四写路径）；`p5-trigger` B9×2（新 trigger 家族持久化+原子）；`p6-dag` B9×1/逐字节×4（import 拒、EX-19c 闲置、EX-20e）；`p3-integration` #15 损坏回退；`security/fuzz` 逐字节×2；`p6-cli` apply 拒零副作用。
- **回滚（既有，指认）**：`config-v2/test.sh` §7 `tcfg_rollback`（备份 config 逐字节恢复+MANAGED 移除→legacy）+ §5 导入失败逐字节+无半成品；`P1/P3/P4-UPGRADE-ROLLBACK.md` 文档面 + `tests/recovery/test.sh` 25/0。
- **I/O 真实写失败演示（本任务净新增）**：p6-verify §atomic——占位 `.tmp.$$` 制造 `printf` 重定向失败 → `tcfg_apply_task` **rc=2**（错误类与校验拒绝 rc=1 可区分，IPC 面转 `configuration_invalid: write failed`，`ipc_op_edit_task` L6143）、目标 task 文件 `cmp` **逐字节不变**、无 tmp 残留。**结论：三层（校验拒/写失败/导入中断）原子性闭环成立。**

---

## 5. 缺陷/观察登记（只登记，不修）

| ID | 面 | 现象（复现） | 证据 | 处置建议 |
| :-- | :-- | :-- | :-- | :-- |
| **O-P6-11-01** | catch-up × 同日回滚 | 新版运行（写 `last_tick=T`）→ 同日回滚旧版（无 last_tick 语义，正常执行并 cycle-mark T+1..窗口）→ 同日再升级新版：新版 catch-up 依据**滞留的 last_tick** 将旧版已执行窗口重放（**非幂等时间任务二次执行**，审计 `catchup=1`）。有界：仅同日且 gap≤CATCHUP_MAX=3 分钟窗口；跨日/跨窗（真实升级重启流）安全（无 last_tick → 不误补，`upg-7` 已证）。宿主复现：`tests/p6-verify/test.sh` upg-5（`ut2` 总执行=2，**当前行为已固化为 LOCK 断言**，语义变化会红提醒同步） | p6-verify upg-1..5；P6-10 设备 §9 场景未覆盖此序（其回滚演练含重启清态语义） | 交人工裁决（候选方案：catch-up 前查 `cycle-<窗口token>` 字面行含该 id 即跳过——与 pending 有效性检查同源；触碰 P6-02 冻结语义，登记不修） |
| O-P7-04 复验注记 | 升级首扫 | 登记波一次性 O(roots) 现图重建+全历史扫描窗：9 根拒新波 pass ~1.7s（32 根档 tick ~1.7–2.2s）；此后稳态 0.35–0.5s | p6-verify §perf3/§conc；resource H5/H6 | 维持原登记（P6-10 设备实测 scanmark 升级首扫耗时并入 §7 设备复验观察项） |

既有登记状态复核：O-P6-10-05（三方竞速，非缺陷）、O-P6-10-06（显示语义 → P7）、O-P7-01..06 无变化；本任务无新增产品缺陷（O-P6-11-01 为第 1 例新增观察）。

---

## 6. 硬门禁结果（全部重跑，WSL 原生 FS，安静窗 load 0.0 起测）

| 门禁命令 | 结果行 |
| :-- | :-- |
| `bash tests/run_tests.sh`（最终版套件聚合复跑） | **ALL SUITES GREEN / exit 0**；**51 套件**、trace `[PASS]`=**2449**、`[FAIL]`=**0**；trace `tests/results/run_tests-20260910-124500.log`（界值调整前的等价前次 51 套件绿 `…-120326.log`；P6-10 基线 50 套件绿 `…-095905.log`） |
| `bash tests/p3-integration/test.sh` | PASS=**49** FAIL=0 |
| `bash tests/p4-dependency/test.sh` | PASS=**220** FAIL=0 |
| `bash tests/p5-condition/test.sh` | PASS=**60** FAIL=0 |
| `bash tests/p5-trigger/test.sh` | PASS=**137** FAIL=0 |
| `bash tests/p6-reliability/test.sh` | PASS=**25** FAIL=0 |
| `bash tests/p6-dag/test.sh` | PASS=**177** FAIL=0 SKIP=0 |
| `bash tests/p1-build/build_check.sh` | PASS=**29** FAIL=0 SKIP=0 |
| `bash tests/resource/stress.sh` | PASS=**26** FAIL=0 |
| `bash tests/security/fuzz.sh` | PASS=**37** FAIL=0 |
| 附加复核 | `tests/ipc/security.sh` 17/0 · `tests/security/path-validation.sh` 9/0 · `tests/legacy/golden.sh` 8/0 · `tests/shadow/test.sh` 23/0 · `tests/idmap/test.sh` 25/0 · `tests/config-v2/test.sh` 44/0 + `validation.sh` 41/0 · `tests/p1-regression/test.sh` 44/0 · `tests/lifecycle-prod/test.sh` 44/0 · `tests/crashguard/test.sh` 60/0 · **`tests/p6-verify/test.sh` 30/0（最终版独立 3 连跑 + 聚合绿；全版本累计 7 次独立全绿、0 误红）** |
| `tests/run_tests.sh --with-device` 三连 | **不执行**——设备离线，列入 §7 延后清单（AGENTS §4.3 允许的唯一跳过项，明示 DEVICE 延后） |

---

## 7. 延后清单（设备重连/后续必做；自 P6-10 延后节并入并补全）

**A. 设备复验（P6-10 三项修复的真机确认，硬性）**
1. 修后 `bash tests/p6-device/smoke.sh --with-device --serial <S>` 真机复跑（安静窗）：
   - **D-P6-10-02**：确定性冷启动收敛后，重启风暴/高负载期复查单实例（status Alive、审计每 token ≤1 行 op=tick）；
   - **D-P6-10-01**：只清 `dag/` 留 `cycle-*` 复现旧饿死场景 → 复验陈旧 token 不重登记、新链可登记（24h 年龄窗）；
   - **D-P6-10-03**：kill×respawn 微窗复跑（run#16 时序）→ 复验 `retry.max=1` 节点 ≤1+max 且终局后零重放。
2. `bash tests/run_tests.sh --with-device` 汇总复跑全绿（p1-device 11/0、p3-device 28/0 段保持；p6-device 九场景）。
3. **§7 清理 runbook**（`docs/P6-10.md §7` 步骤 1..5 原样执行：复位残留 MANAGED/`p6_*` 任务/dag/p6-snap 备份；**关键风险**：`$TCFG/p6_*.task` 含分钟触发 HHMM 任务，到点会真执行——先复位再复跑；孪生守护复查纳入步骤 3）。

**B. shell 兼容真运行时**
4. **mksh 真机复验**：宿主无 mksh runtime（不 apt install）；P5-09 先例法已跑（D36/D37 静态结构断言含 P6 全部新增代码入扫描面 + dash -n/dash 真跑），**Android 16 mksh 为最终裁判**——p6-device 复跑即覆盖。

**C. 发布矩阵**
5. **Magisk / APatch / 多 Android 版本矩阵 14 格**（`docs/P6-10-DEVICE-MATRIX.md` §1.1 诚实面：本机仅 KernelSU×A16 已验证，其余 14 格⏳发布限制不得伪造；建议充电+锁屏静置/飞行条件跑）。

**D. CI 演练面（本任务新识别）**
6. `tests/p6-verify/test.sh` §upgrade 在 CI（`actions/checkout@v4` 默认 `fetch-depth:1`，且 tag `p5-baseline-*` 未推 origin）下自动 **[SKIP]**；如需 CI 真跑：test.yml checkout 加 `fetch-depth:0`（或推 tag）——属 CI 配置变更，**待人工裁决**，宿主全历史环境（本地 WSL/git 仓）已实测 8/8 绿。

**E. 遗留项（P6-10 结转，未修/降级，非缺陷）**
7. O-P6-10-05（真机控制三方竞速）——设计面既定语义，无产品改动；O-P6-10-06（`root=` 行 STOPPED 与 run SUCCESS 并存的展示语义）——登记 P7；D-P6-10-01 年龄窗 24h 取值依据（不误伤 H5 合法跨日补登 ~930min）维持。

---

## 8. 遗留风险

1. **设备证据缺口**：§7-A 三项修后真机复验、--with-device 组合、14 格矩阵未跑——P6 出口（发布判据）在设备重连前**不得视为完成**；宿主证据（孪生围栏/年龄钳/终局 exhaust 的语义面）已由 lifecycle-prod §8(12)/p6-dag §D01(9)/crashguard §16(6) 先行锁定。
2. **O-P6-11-01**：同日回滚再升级的 catch-up 重放窗口（≤3 分钟）——非幂等任务在最坏情形二次执行；真实升级路径（重启）不触发；已 LOCK 固化防无声漂移。
3. **tick 近界抖动**：50 任务决策档（~1700/2500）与链档（~1700）在高负载宿主仍可能整体抬升——p6-verify 已内置 A/B 地板（两档同涨不误红，病态独涨必红）；`p6-reliability` §perf 维持原 ≤2000 界（既有测试不可改纪律，P6-09 A/B 取证流程沿用）。
4. **IPC 延迟语义**：基线值受「服务端 1s poll + 客户端 1s 轮询」双重粒度支配，WebUI/CLI 感知时延上界 ~2s 为设计成本；若未来要求亚秒响应（P1+ 项），需改 poll 模型——不在 P6 范围。
5. **mksh 为推断面**：dash 真跑+静态断言是代理证据；真机（B4）通过前，「mksh 兼容」结论标记为「结构断言成立、运行时待证」。

---

## 9. 约束自查（C1–C5 对齐）

| 约束 | 核查 | 结果 |
| :-- | :-- | :-- |
| C1 最小 diff | 生产零改动；测试净新增 1 文件（30 断言）+ run_tests +4 行注册 | ✅（`git diff --stat`：仅 `tests/run_tests.sh +4`） |
| C2 旧路径零删除 | 未触碰任何解析/执行路径；既有断言零改（run_tests diff 无删除行） | ✅ |
| C3 零新依赖 | 新套件仅用 `bash/dash -n`、`node --check`（工具在场守卫）、`git show`（历史资产提取）；生产运行期零变化 | ✅ |
| C4 配置格式零变更 | fixtures/触发格式/modifiers 零触碰；`legacy/golden` 8/0、`p5-*` 全绿佐证 | ✅ |
| C5 不提前实现 | 零 WebUI/Watchdog/Dependency 实现或占位（p6-verify 仅读 dag/ 数据面断言） | ✅ |
| 未 commit/push | 工作树仅上述测试+docs 改动 | ✅ |
