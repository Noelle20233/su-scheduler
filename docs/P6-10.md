# Su Scheduler — P6-10 执行记录：设备矩阵扩展（真机验证）

> **任务**：P6-10 · 设备矩阵扩展（九必测场景真机固化 + tests/p6-device/smoke.sh + 矩阵文档）
> **前置**：P6-01..09（HEAD `ff17bf9`，Runtime 1.32.0，宿主基线全绿）
> **执行**：2026-09-09 18:00 ～ 2026-09-10 05:1x（跨夜；真机 KernelSU × Android 16，
> 有线 `8934ffc4` → 后半夜物理掉线回退同机无线 `192.168.2.156:xxxxx`）
> **纪律**：生产代码零改动（本轮实测确认，见 §6）；设备缺陷只登记不顺手修；测试侧
> 时序问题修测试并注明；不 commit/push。

---

## 1. 交付物

| 交付物 | 状态 |
| :--- | :--- |
| `tests/p6-device/smoke.sh`（九场景固化，56 断言；`--with-device/--skip-device/--serial`、ANDROID_SERIAL、DEVICE_SKIPPED、SUITE_TIMEOUT 说明、配置自愈比对、trace 落盘） | ✅ 新建 |
| `tests/run_tests.sh`：`--with-device` 注册 p6-device（p1→p3→p6 顺序，超时 3000s） | ✅ 修改（仅注释+注册行） |
| `docs/P6-10-DEVICE-MATRIX.md`（矩阵 + 九场景证据 + 14 格资源探查/发布限制 + 升降级演练记录） | ✅ 新建 |
| `docs/P6-10.md`（本文档） | ✅ |
| 升级/回滚真机演练（P6 1.32.0 → P5 1.30.0 → P6） | ✅ 完整一轮通过（矩阵 §4） |

## 2. 验收命令与结果（逐条）

| 验收命令 | 结果 | 证据 |
| :-- | :-- | :-- |
| `bash tests/p6-device/smoke.sh --with-device`（有线 run#18） | **PASS=56 FAIL=0 SKIP=0**，1692s | `tests/results/device-8934ffc4-20260909-220714-p6.log` |
| `bash tests/p3-device/smoke.sh --with-device` | **28/0/0/BLOCKED=0**（22:4x 独立；23:20 汇总内、02:16 汇总内复现；负载风暴期 03:33 一次 26/2=环境项，同断言三跑全绿） | trace `device-*-20260909-232012.log` / `device-*-20260910-021619.log` |
| p1-device legacy 冒烟 | **11/0/2SKIP 恒定**（独立 + 全部四次汇总内） | trace 002926 等 |
| `bash tests/run_tests.sh`（宿主 L1+L2+L4） | **ALL SUITES GREEN / exit 0**（50 套件） | `tests/results/run_tests-20260910-051105.log` |
| `bash tests/p6-dag/test.sh` | **168/0/0** | §独立复跑（22:5x） |
| `bash tests/p6-reliability/test.sh` | **25/0** | 同上 |
| `bash tests/run_tests.sh --with-device` 汇总 | **未达全绿**（四次尝试，全部败于 ①物理链路掉线（USB 17:57/01:15、无线 04:20，04:5x 起设备整机不可达）或 ②用户 App 负载风暴期 rc5/tick 膨胀——同期连既有 p3 也 26/2）；设备安静时段的等价证据：p1 11/0 + p3 28/0 + p6 56/0 三项同机同日全部达成（矩阵 §0/§5.3） | `run_tests-20260909-230114.log`、`run_tests-20260910-{000811,015943,031136}.log` |
| 升级/回滚演练（矩阵 §4 全表） | ✅ RB-1..6 全过 + 升回 1.32.0 复验（boot 后 runtime=1.32.0、Alive、config d41d8… 逐字节） | 设备 log `/data/adb/su-scheduler/su-scheduler.log` + 演练输出 |

**汇总项判定**：套件逻辑与断言已实证（安静窗口全绿）；全绿汇总受设备物理条件阻断，
属 §3 环境限制而非产品/测试缺陷 → 遗留待设备可稳定唤醒时复跑（预期全绿；p6 段窗口
已按负载实测放大）。

**设备最终状态（05:39）**：自约 04:37 起有线 `8934ffc4` 与同机无线 `192.168.2.156:*`
**双双从 `adb devices` 消失**（`adb kill-server` 重启 + mDNS 重扫 + `adb connect` 端口
拒绝均无果），判定为设备进入深度休眠或被物理移出/断电，工具侧无法唤醒。最后一次
`--with-device` 汇总（`run_tests-20260910-031136.log`）在 p6 wave-A 期设备掉线 → 判
TIMEOUT（**非套件缺陷**：同断言 run#18 56/0/0 绿、p3 26/2 亦为负载/掉线环境项）。
设备残留（MANAGED + 本日 `p6_*`/`p6_s*` 任务 + dag/ + p6-snap 备份）由 §7 runbook 在
设备恢复连接后一次性清理（含明日 HHMM 到点复跑风险规避）。本轮 Windows 侧临时
helper 与 `.p6tmp/` 已删；**生产文件零改动**（仅 `tests/run_tests.sh` 注册 + 新增
`tests/p6-device/` + 两份文档），未 commit/push。

## 3. 九场景执行摘要（详见矩阵 §2）

跳拍补偿 ✅｜Cron ID ✅｜IPC 错误透传 ✅｜DAG（线性/合流/失败传播/配置期拒/chain 子命令）✅｜
Retry/Recovery/Crash Loop ✅（例外登记 D-P6-10-03）｜WebUI 查询+控制（波 B EX-12 确定性 +
批量 + dag 键）✅｜daemon 重启恢复（run 中途 kill 账本存续、零重放/零重派发）✅｜
Legacy 回归（p1 承载）✅｜升级与回滚 ✅。

## 4. 缺陷/观察登记（本轮新增，均不顺手修）

| ID | 级别 | 现象（证据） | 处置建议 |
| :-- | :-- | :-- | :-- |
| **D-P6-10-01** | 引擎·登记扫描 | 只清 `dag/` 不清 `cycle-*`/`last_tick` 时，历史 token 被 F2 有界扫描重登记为 PENDING 僵尸 run，占满 `DAG_RUNS_MAX=8` 名额 → 新链饿死（run#10 前发现） | 套件 preflight 已复位三件套（测试面）；产品面：扫描加窗口/年龄钳制 |
| **D-P6-10-02** | 锁接管竞态 | 高负载 cmd_stop 6s 超时删锁 → 新实例 noclobber 接管成功 + **旧非持有者残留** → 双/三 daemon 同 tick（审计每分钟 2–3 行，02:10–02:23 取证）；**冷启动收敛后重启风暴期仍可再起**（run#18 后证） | 套件已改确定性冷启动 + 波 B 前补扫（测试面）；产品面专项（锁持有者唯一性/接管仲裁）待排期 |
| **D-P6-10-03** | 重试钳制缺口 | kill -9 落在节点重试 respawn 后数秒的微窗 → 恢复判该次为 daemon_restart 残留 + 清 retry 计数 → `retry.max=1` 节点执行 **3 次**（1+max+1），且发生在 run 已终局 FAILED 之后（run#16：21:02:14 start / 21:02:18 kill / 21:03:10 complete / 21:04:08 re-arm attempt=1 / 21:05:08 第 3 跑）| 套件规避（kill 点等 rc 链终局）；**有界非无界**；需 T2 式最小复现（宿主 mock 同窗）+ 修复（replenish 应无视残存 retry.count=0，以 events.log 计数为准） |
| **O-P6-10-04** | 文档管线 | `build.sh` 由 README **再生** `.su-scheduler-docs`：P6-09 对 HEAD 文件的手工追加（chain 行等）不在 README → 构建即 dirty 工作树（曾误判为污染）且出厂 zip 内文档缺 P6-09 段落（zip 实测 chain_line=0）；构建输出反含 README 独有的 P1-13 章节而 HEAD 文件无 → 双向漂移 | 属文档管线一致性（T4/P1 域）；本轮仅记录。建议后续：文档单一来源（README）+ 构建后提交再生文件 |
| **O-P6-10-05** | 真机控制竞态（设计面） | kill 波内 manual stop 需赢过恢复首拍 residual——三方竞速（kill×startup IPC 饥饿×residual），不可稳定取胜（#10 赢 / #15/16/17 输）；R7 对**在途节点判 FAILED（活子进程收养、不重派发）为既定语义**，「kill 时在途 + 合流 SUCCESS」不可同窗 | 套件已重构：R7 证据在 kill 波（零重派发）、EX-12 证据在无 kill 波 B（确定性）；无产品改动 |
| **O-P6-10-06** | 链账本细节 | `root=` 行状态在根任务自然完成后长期显示 `STOPPED`（镜像 state.txt 收割态），run 级 state 独立正确（SUCCESS）；`chain` 渲染 root_state=STOPPED 与 run SUCCESS 并存易误读 | 展示语义微调候选（P7 登记）；非缺陷 |

**宿主/环境记录（非缺陷）**：WSL adb=interop（push 不认 /mnt 路径）；WSL VM 空闲回收
杀后台 run（run#11 亡）→ 长套件前台+home 落日志；`grep '$'` 锚经 adb→mksh 双引号层
变 ANSI-C 解析（pred2 实证）→ 判据只用左锚；设备时钟窗避开 23:15–00:40 起波。

## 5. 套件迭代记录（p6-device，全部为测试侧修复）

| run | 结果 | 变更/事件 |
| :-- | :-- | :-- |
| #10（前段无线） | 48/4 | 4-ipcerr 透传通道定型；tick 聚合判据晚读教训 |
| #12 | 47/6 | kill 判据改「账本定稿」（marker 判据输掉 R7 竞态） |
| #13 | 49/1+1skip | 发现 run.txt 成员行=CLI 渲染格式误用（恒不命中）→ pk-probe 取证真实格式 |
| #14 | 50/0+1skip | predicate 格式修正中；kill 落 post-close 窗（8-control 诚实 SKIP） |
| #15 | 47/6 | 镜像 STOPPED ≠ 收工（定稿拍同窗仍 FAILED）→ 三条件 kill 点 |
| #16 | 49/4 | cf 钳制被 kill 微窗击穿 → **D-P6-10-03 取证**；R7 澄清（run#10=相位运气） |
| #17 | 50/3 | 五条件 kill 点 + rc5 快回重试族 |
| **#18** | **56/0/0** | kill 波（R7 零重派发）+ **波 B**（EX-12 确定性 stop→合流 SUCCESS）全绿 |
| 汇总内 #A..#D | 50/6、TIMEOUT、51/5、TIMEOUT | 链路掉线 ×2 + 用户 App 负载风暴（连 p3 也 26/2）→ 后续加固：[env] 负载记录/沉降、ipc_ready 200s、波 B 前补扫+settle 门、窗口 480/300/420→放宽 |

（#18 后追加的加固未再获设备窗口复跑——逻辑均沿用已验证判据，仅扩窗/重试/记录，
风险面=纯测试侧时序容差；下次设备可用时直接复跑验证即可。）

## 6. 约束自查与现场

| 项 | 核查 | 结果 |
| :-- | :-- | :-- |
| C1/生产零改动 | `git diff` 范围 = `tests/run_tests.sh`（注册+注释）+ 新增 `tests/p6-device/smoke.sh` + 两个新文档；`system/bin/*`、`service.sh`、`build.sh` 等**零触碰** | ✅ |
| C2/C4 语义 | 未触解析/执行路径；config 格式零改动（真机配置逐字节比对恒等） | ✅ |
| C3 依赖 | 套件仅用 adb + POSIX + 仓库既有工具 | ✅ |
| 既有套件零回退 | p1 恒 11/0；p3 恒 28/0（除 26/2 环境样本，见 §2）；宿主 50 套件全绿；p6-dag 168/0、p6-reliability 25/0 | ✅ |
| 现场恢复 | 见 §7 —— 模块=最新构建（演练后已升回并复验）；config=空基线（d41d8…）；**03:3x 汇总强杀遗留的 p6 工件清理因设备掉线未完成**（§7 runbook） | ⚠️ 部分（设备失联） |
| commit/push | 未执行 | ✅ |

## 7. 遗留与恢复 runbook（设备重新上线后必做）

设备于 04:37 起整机不可达（USB+WiFi adb 均断）。最后一次被强杀的 p6 运行（03:3x，
无线）在设备上可能残留：`$TCFG/p6_*.task`（含**明日分钟触发的 HHMM 任务**，若
MANAGED 旗标在位则到点会真实执行 echo 到 /data/local/tmp）、`$TCFG/MANAGED`、
`$DATA/dag/`、`/data/local/tmp/p6-*`；`/data/local/tmp/p6-snap/` 内三枚演练
vault tgz + config 快照（备份清理未执行）；孪生守护需复查。

**恢复步骤**（root 设备上执行；亦是 `10-restore` 用例语义）：
1. `adb devices` 确认 `8934ffc4`（有线优先）→ `su -c 'cat /data/adb/su-scheduler/task-config/MANAGED 2>/dev/null; ls /data/adb/su-scheduler/task-config | head'` 盘点残留。
2. 复位：`rm -f /data/adb/su-scheduler/task-config/MANAGED /data/adb/su-scheduler/task-config/p6_*.task; rm -rf /data/adb/su-scheduler/dag /data/adb/su-scheduler/tasks/p6_*; rm -f /data/adb/su-scheduler/scheduler/cycle-* /data/adb/su-scheduler/scheduler/last_tick; rm -f /data/local/tmp/p6-m-* /data/local/tmp/p6-cf-exec /data/local/tmp/p6*.pay*`
3. 冷启动收敛（清 `/dev/.su_scheduler.lock` + `runtime/daemon.guard` → 直启单实例 → 扫非持有者），核对 `status` Alive、单实例、`task-config status`=legacy、`config.txt` md5=`d41d8cd98f00b204e9800998ecf8427e`（若异：`cp /data/local/tmp/p6-snap/config.pre-rollback <config>` 后再删快照）。
4. 备份清理：`rm -rf /data/local/tmp/p6-snap /data/local/tmp/_p6_rb_*.sh /data/local/tmp/ss-mod-p*.zip`。
5. 复跑验收（建议顺序）：`bash tests/p6-device/smoke.sh --with-device --serial <S>`（安静窗）→ `bash tests/run_tests.sh --with-device`（预期全绿）。

Windows 侧遗留（本次收尾已删/保留说明）：`.p6tmp/` 演练 zip 与全部 `_p6*`/`h_*` helper
脚本按纪律删除；恢复 runbook 已文档化于此，不依赖脚本残留。

## 8. 与 P6-09 移交项的对齐

P6-08 §4 移交的「GET_SUMMARY.dag 真机时延（O-P7-05）」已实测：quiet 15–58s /
负载期 >80s×5 全 rc5（矩阵 §6）→ 建议 P7 评估 dag 聚合分片/缓存。P6-02 跨午夜
不补偿语义与套件时钟窗（23:15–00:40 规避）已在套件头注记录。
