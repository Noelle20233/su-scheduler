# Su Scheduler — P6-10 执行记录：设备矩阵扩展（真机验证）

> **任务**：P6-10 · 设备矩阵扩展（九必测场景真机固化 + tests/p6-device/smoke.sh + 矩阵文档）
> **前置**：P6-01..09（HEAD `ff17bf9`，Runtime 1.32.0，宿主基线全绿）
> **执行**：2026-09-09 18:00 ～ 2026-09-10 05:1x（跨夜；真机 KernelSU × Android 16，
> 有线 `8934ffc4` → 后半夜物理掉线回退同机无线 `192.168.2.156:xxxxx`）
> **纪律**：生产代码零改动（本轮实测确认，见 §6）；设备缺陷只登记不顺手修；测试侧
> 时序问题修测试并注明；不 commit/push。

---

## 1. 交付物

| 交付物 | 状态 |
| :--- | :--- |
| `tests/p6-device/smoke.sh`（九场景固化，56 断言；`--with-device/--skip-device/--serial`、ANDROID_SERIAL、DEVICE_SKIPPED、SUITE_TIMEOUT 说明、配置自愈比对、trace 落盘） | ✅ 新建 |
| `tests/run_tests.sh`：`--with-device` 注册 p6-device（p1→p3→p6 顺序，超时 3000s） | ✅ 修改（仅注释+注册行） |
| `docs/P6-10-DEVICE-MATRIX.md`（矩阵 + 九场景证据 + 14 格资源探查/发布限制 + 升降级演练记录） | ✅ 新建 |
| `docs/P6-10.md`（本文档） | ✅ |
| 升级/回滚真机演练（P6 1.32.0 → P5 1.30.0 → P6） | ✅ 完整一轮通过（矩阵 §4） |

## 2. 验收命令与结果（逐条）

| 验收命令 | 结果 | 证据 |
| :-- | :-- | :-- |
| `bash tests/p6-device/smoke.sh --with-device`（有线 run#18） | **PASS=56 FAIL=0 SKIP=0**，1692s | `tests/results/device-8934ffc4-20260909-220714-p6.log` |
| `bash tests/p3-device/smoke.sh --with-device` | **28/0/0/BLOCKED=0**（22:4x 独立；23:20 汇总内、02:16 汇总内复现；负载风暴期 03:33 一次 26/2=环境项，同断言三跑全绿） | trace `device-*-20260909-232012.log` / `device-*-20260910-021619.log` |
| p1-device legacy 冒烟 | **11/0/2SKIP 恒定**（独立 + 全部四次汇总内） | trace 002926 等 |
| `bash tests/run_tests.sh`（宿主 L1+L2+L4） | **ALL SUITES GREEN / exit 0**（50 套件） | `tests/results/run_tests-20260910-051105.log` |
| `bash tests/p6-dag/test.sh` | **168/0/0** | §独立复跑（22:5x） |
| `bash tests/p6-reliability/test.sh` | **25/0** | 同上 |
| `bash tests/run_tests.sh --with-device` 汇总 | **未达全绿**（四次尝试，全部败于 ①物理链路掉线（USB 17:57/01:15、无线 04:20，04:5x 起设备整机不可达）或 ②用户 App 负载风暴期 rc5/tick 膨胀——同期连既有 p3 也 26/2）；设备安静时段的等价证据：p1 11/0 + p3 28/0 + p6 56/0 三项同机同日全部达成（矩阵 §0/§5.3） | `run_tests-20260909-230114.log`、`run_tests-20260910-{000811,015943,031136}.log` |
| 升级/回滚演练（矩阵 §4 全表） | ✅ RB-1..6 全过 + 升回 1.32.0 复验（boot 后 runtime=1.32.0、Alive、config d41d8… 逐字节） | 设备 log `/data/adb/su-scheduler/su-scheduler.log` + 演练输出 |

**汇总项判定**：套件逻辑与断言已实证（安静窗口全绿）；全绿汇总受设备物理条件阻断，
属 §3 环境限制而非产品/测试缺陷 → 遗留待设备可稳定唤醒时复跑（预期全绿；p6 段窗口
已按负载实测放大）。

**设备最终状态（05:39）**：自约 04:37 起有线 `8934ffc4` 与同机无线 `192.168.2.156:*`
**双双从 `adb devices` 消失**（`adb kill-server` 重启 + mDNS 重扫 + `adb connect` 端口
拒绝均无果），判定为设备进入深度休眠或被物理移出/断电，工具侧无法唤醒。最后一次
`--with-device` 汇总（`run_tests-20260910-031136.log`）在 p6 wave-A 期设备掉线 → 判
TIMEOUT（**非套件缺陷**：同断言 run#18 56/0/0 绿、p3 26/2 亦为负载/掉线环境项）。
设备残留（MANAGED + 本日 `p6_*`/`p6_s*` 任务 + dag/ + p6-snap 备份）由 §7 runbook 在
设备恢复连接后一次性清理（含明日 HHMM 到点复跑风险规避）。本轮 Windows 侧临时
helper 与 `.p6tmp/` 已删；**生产文件零改动**（仅 `tests/run_tests.sh` 注册 + 新增
`tests/p6-device/` + 两份文档），未 commit/push。

## 3. 九场景执行摘要（详见矩阵 §2）

跳拍补偿 ✅｜Cron ID ✅｜IPC 错误透传 ✅｜DAG（线性/合流/失败传播/配置期拒/chain 子命令）✅｜
Retry/Recovery/Crash Loop ✅（例外登记 D-P6-10-03）｜WebUI 查询+控制（波 B EX-12 确定性 +
批量 + dag 键）✅｜daemon 重启恢复（run 中途 kill 账本存续、零重放/零重派发）✅｜
Legacy 回归（p1 承载）✅｜升级与回滚 ✅。

## 4. 缺陷/观察登记（本轮新增，均不顺手修）

> **P6-10-fix 状态更新（设备离线期宿主修复）**：D-P6-10-01/02/03 + O-P6-10-04 已按 T2 模式
> （宿主先 FAIL 复现 → 最小修复 → FAIL 转 PASS → 门禁回归全绿）在后续专项任务中修复，
> 修复要点与测试引用见下表「处置建议」列；三产品缺陷的**设备真机复验延后**（待设备重连），
> 延后事项与 §7 清理 runbook 记录于 **`docs/P6-11.md`**。本轮（P6-10）实测时生产代码仍零改动。

| ID | 级别 | 现象（证据） | 处置建议 |
| :-- | :-- | :-- | :-- |
| **D-P6-10-01** | 引擎·登记扫描 | 只清 `dag/` 不清 `cycle-*`/`last_tick` 时，历史 token 被 F2 有界扫描重登记为 PENDING 僵尸 run，占满 `DAG_RUNS_MAX=8` 名额 → 新链饿死（run#10 前发现） | **已修（P6-10-fix）**：§27 `dag_scan_registrations` 登记候选加年龄/可解析钳制（新原语 `dag_token_age_le`，默认窗 `DAG_SCAN_MAX_AGE_MIN=1440` 分钟）——陈旧残留 token 跳过登记、不回 pending。测试 `tests/p6-dag/test.sh §D01`（DAG-D01-01..07）先 FAIL 后 PASS；preflight 复位保留（双保险）|
| **D-P6-10-02** | 锁接管竞态 | 高负载 cmd_stop 6s 超时删锁 → 新实例 noclobber 接管成功 + **旧非持有者残留** → 双/三 daemon 同 tick（审计每分钟 2–3 行，02:10–02:23 取证）；**冷启动收敛后重启风暴期仍可再起**（run#18 后证）| **已修（P6-10-fix）**：(b) daemon 主循环顶加锁主围栏（新原语 `lifecycle_lock_owner_fence`：锁内 PID 为另一存活 su-schedulerd → 非持有者自退，不删锁、记 clean exit）；(a) CLI `cmd_stop` 仅在确认持有者死亡后才删锁，超时存活则报错返回（保留锁）。测试 `tests/lifecycle-prod/test.sh §8`（D-P6-10-02 A/B/C）先 FAIL（B1 双实例 2 行 op=tick）后 PASS；service.sh 零改动。设备真机复验延后（见 docs/P6-11.md）|
| **D-P6-10-03** | 重试钳制缺口 | kill -9 落在节点重试 respawn 后数秒的微窗 → 恢复判该次为 daemon_restart 残留 + 清 retry 计数 → `retry.max=1` 节点执行 **3 次**（1+max+1），且发生在 run 已终局 FAILED 之后（run#16：21:02:14 start / 21:02:18 kill / 21:03:10 complete / 21:04:08 re-arm attempt=1 / 21:05:08 第 3 跑）| **已修（P6-10-fix）**：§27 `dag_retry_replenish` 以 events.log **全部**失败事件（`action_failure`+`daemon_restart`+`timeout`）计"已消耗尝试"，不信任残存/清零的 retry.count；run 终局分支改调新原语 `dag_retry_terminate`（count:=max + 清 retry.until），引擎侧拒绝终局后重放。测试 `tests/crashguard/test.sh §16 G4`（先 FAIL `cf_ex=3`→后 PASS ≤2）+ G4b..f；改动限 §27（未碰 state_sync_one/sched_retry_arm 本体）。设备真机复验延后（见 docs/P6-11.md）|
| **O-P6-10-04** | 文档管线 | `build.sh` 由 README **再生** `.su-scheduler-docs`：P6-09 对 HEAD 文件的手工追加（chain 行等）不在 README → 构建即 dirty 工作树（曾误判为污染）且出厂 zip 内文档缺 P6-09 段落（zip 实测 chain_line=0）；构建输出反含 README 独有的 P1-13 章节而 HEAD 文件无 → 双向漂移 | 属文档管线一致性（T4/P1 域）；**已修（P6-10-fix）**：P6-08/09 的 CLI 文档增量（`chain` 行 + 查询示例块）回灌 README（唯一权威源），重新 `bash build.sh` 再生 `.su-scheduler-docs`（与 README 逐字节一致、二次构建幂等）；zip 内 docs `su-scheduler chain` 计数 0→2。新增断言入 `tests/p1-build/build_check.sh`（zip docs 含 chain + 再生幂等），并把构建后 `git checkout -- docs` 改为「快照还原」（避免退回 HEAD 陈旧 docs 反引入漂移）。未改 build.sh 逻辑|
| **O-P6-10-05** | 真机控制竞态（设计面） | kill 波内 manual stop 需赢过恢复首拍 residual——三方竞速（kill×startup IPC 饥饿×residual），不可稳定取胜（#10 赢 / #15/16/17 输）；R7 对**在途节点判 FAILED（活子进程收养、不重派发）为既定语义**，「kill 时在途 + 合流 SUCCESS」不可同窗 | 套件已重构：R7 证据在 kill 波（零重派发）、EX-12 证据在无 kill 波 B（确定性）；无产品改动 |
| **O-P6-10-06** | 链账本细节 | `root=` 行状态在根任务自然完成后长期显示 `STOPPED`（镜像 state.txt 收割态），run 级 state 独立正确（SUCCESS）；`chain` 渲染 root_state=STOPPED 与 run SUCCESS 并存易误读 | 展示语义微调候选（P7 登记）；非缺陷 |

**宿主/环境记录（非缺陷）**：WSL adb=interop（push 不认 /mnt 路径）；WSL VM 空闲回收
杀后台 run（run#11 亡）→ 长套件前台+home 落日志；`grep '$'` 锚经 adb→mksh 双引号层
变 ANSI-C 解析（pred2 实证）→ 判据只用左锚；设备时钟窗避开 23:15–00:40 起波。

## 5. 套件迭代记录（p6-device，全部为测试侧修复）

| run | 结果 | 变更/事件 |
| :-- | :-- | :-- |
| #10（前段无线） | 48/4 | 4-ipcerr 透传通道定型；tick 聚合判据晚读教训 |
| #12 | 47/6 | kill 判据改「账本定稿」（marker 判据输掉 R7 竞态） |
| #13 | 49/1+1skip | 发现 run.txt 成员行=CLI 渲染格式误用（恒不命中）→ pk-probe 取证真实格式 |
| #14 | 50/0+1skip | predicate 格式修正中；kill 落 post-close 窗（8-control 诚实 SKIP） |
| #15 | 47/6 | 镜像 STOPPED ≠ 收工（定稿拍同窗仍 FAILED）→ 三条件 kill 点 |
| #16 | 49/4 | cf 钳制被 kill 微窗击穿 → **D-P6-10-03 取证**；R7 澄清（run#10=相位运气） |
| #17 | 50/3 | 五条件 kill 点 + rc5 快回重试族 |
| **#18** | **56/0/0** | kill 波（R7 零重派发）+ **波 B**（EX-12 确定性 stop→合流 SUCCESS）全绿 |
| 汇总内 #A..#D | 50/6、TIMEOUT、51/5、TIMEOUT | 链路掉线 ×2 + 用户 App 负载风暴（连 p3 也 26/2）→ 后续加固：[env] 负载记录/沉降、ipc_ready 200s、波 B 前补扫+settle 门、窗口 480/300/420→放宽 |

（#18 后追加的加固未再获设备窗口复跑——逻辑均沿用已验证判据，仅扩窗/重试/记录，
风险面=纯测试侧时序容差；下次设备可用时直接复跑验证即可。）

## 6. 约束自查与现场

| 项 | 核查 | 结果 |
| :-- | :-- | :-- |
| C1/生产零改动 | `git diff` 范围 = `tests/run_tests.sh`（注册+注释）+ 新增 `tests/p6-device/smoke.sh` + 两个新文档；`system/bin/*`、`service.sh`、`build.sh` 等**零触碰** | ✅ |
| C2/C4 语义 | 未触解析/执行路径；config 格式零改动（真机配置逐字节比对恒等） | ✅ |
| C3 依赖 | 套件仅用 adb + POSIX + 仓库既有工具 | ✅ |
| 既有套件零回退 | p1 恒 11/0；p3 恒 28/0（除 26/2 环境样本，见 §2）；宿主 50 套件全绿；p6-dag 168/0、p6-reliability 25/0 | ✅ |
| 现场恢复 | 见 §7 —— 模块=最新构建（演练后已升回并复验）；config=空基线（d41d8…）；**03:3x 汇总强杀遗留的 p6 工件清理因设备掉线未完成**（§7 runbook） | ⚠️ 部分（设备失联） |
| commit/push | 未执行 | ✅ |

> **✅ 2026-09-10 16:0x–18:3x 已执行（P6-11 延后设备复验任务）**：下列步骤 1..3 已按
> runbook 原样完成（网络 adb 同机 `192.168.2.156:36777`），残留=MANAGED+
> `p6_cupt1/p6_cupton`（HHMM 到点复跑风险，已先复位再动其他）、`cycle-202609100354`、
> p6-snap 三枚 vault 已留证（config md5 基线核对后方进入清理，收尾执行）；
> 证据与追加动作见 **§9**（含无重启的修后构建落地方式）。
> 本节保留原文作为 runbook 本体。

## 7. 遗留与恢复 runbook（设备重新上线后必做）

设备于 04:37 起整机不可达（USB+WiFi adb 均断）。最后一次被强杀的 p6 运行（03:3x，
无线）在设备上可能残留：`$TCFG/p6_*.task`（含**明日分钟触发的 HHMM 任务**，若
MANAGED 旗标在位则到点会真实执行 echo 到 /data/local/tmp）、`$TCFG/MANAGED`、
`$DATA/dag/`、`/data/local/tmp/p6-*`；`/data/local/tmp/p6-snap/` 内三枚演练
vault tgz + config 快照（备份清理未执行）；孪生守护需复查。

**恢复步骤**（root 设备上执行；亦是 `10-restore` 用例语义）：
1. `adb devices` 确认 `8934ffc4`（有线优先）→ `su -c 'cat /data/adb/su-scheduler/task-config/MANAGED 2>/dev/null; ls /data/adb/su-scheduler/task-config | head'` 盘点残留。
2. 复位：`rm -f /data/adb/su-scheduler/task-config/MANAGED /data/adb/su-scheduler/task-config/p6_*.task; rm -rf /data/adb/su-scheduler/dag /data/adb/su-scheduler/tasks/p6_*; rm -f /data/adb/su-scheduler/scheduler/cycle-* /data/adb/su-scheduler/scheduler/last_tick; rm -f /data/local/tmp/p6-m-* /data/local/tmp/p6-cf-exec /data/local/tmp/p6*.pay*`
3. 冷启动收敛（清 `/dev/.su_scheduler.lock` + `runtime/daemon.guard` → 直启单实例 → 扫非持有者），核对 `status` Alive、单实例、`task-config status`=legacy、`config.txt` md5=`d41d8cd98f00b204e9800998ecf8427e`（若异：`cp /data/local/tmp/p6-snap/config.pre-rollback <config>` 后再删快照）。
4. 备份清理：`rm -rf /data/local/tmp/p6-snap /data/local/tmp/_p6_rb_*.sh /data/local/tmp/ss-mod-p*.zip`。
5. 复跑验收（建议顺序）：`bash tests/p6-device/smoke.sh --with-device --serial <S>`（安静窗）→ `bash tests/run_tests.sh --with-device`（预期全绿）。

Windows 侧遗留（本次收尾已删/保留说明）：`.p6tmp/` 演练 zip 与全部 `_p6*`/`h_*` helper
脚本按纪律删除；恢复 runbook 已文档化于此，不依赖脚本残留。

## 8. 与 P6-09 移交项的对齐

P6-08 §4 移交的「GET_SUMMARY.dag 真机时延（O-P7-05）」已实测：quiet 15–58s /
负载期 >80s×5 全 rc5（矩阵 §6）→ 建议 P7 评估 dag 聚合分片/缓存。P6-02 跨午夜
不补偿语义与套件时钟窗（23:15–00:40 规避）已在套件头注记录。

## 9. P6-11 延后设备复验执行记录（2026-09-10 16:0x–20:2x，网络 adb 192.168.2.156:36777）

### 9.1 清理 runbook（§7 步骤执行对照）

| 步骤 | 结果 |
| :-- | :-- |
| 1 盘点 | 残留实测=**MANAGED + p6_cupt1/p6_cupton（HHMM 到点复跑风险，最先复位）**+ `cycle-202609100354` + `tasks/p6_m-*` 1 枚 + p6-snap 三枚 vault；dag/ 不存在；config md5=d41d8…（基线，无需快照还原）。留证：`tests/results/device-192.168.2.156:36777-20260910-160118-precleanup-{audit.log,inventory.txt}` |
| 2 复位 | MANAGED/p6_*.task/dag/cycle-*/last_tick/tasks/p6_*/tmp p6-* 全清 ✓（备份/manifest 按 runbook 保留；task-config 残留 manifest 于收尾另清） |
| 3 冷启动收敛 | 单实例 ✓ Alive ✓ mode=legacy ✓ config d41d8… ✓ |
| 4 备份清理 | p6-snap 三枚 vault + 本轮新增模块目录快照 `module-dir-pre-dff2b55` 保留至收尾统一清理（config 基线已先行核对） |
| 5 复跑验收 | 见 9.2/9.3 |

### 9.2 修后构建落地方式（无重启——本会话关键环境事实）

设备上模块为 **P6-10 时代 pre-fix 构建（Runtime 同为 1.32.0，代码面 0/0/0 命中三修复
原语）**——版本恒等、代码有差，只能按原语 grep 判别。本机 KernelSU 为 **boot 时
tmpfs/overlay 拷贝**模式（`/system/bin` = `KSU overlay ro, lowerdir=/mnt/vendor/<rand>/bin
:/system/bin`，upper 目录在 ksud 私有 ns 不可写），**非文件 bind**——改模块目录不影响
当机会话视图。reboot 会丢无线 adb（`service.adb.tcp.port` 未持久化，端口不可预测）
→ 采**双轨升级**：①模块目录四文件同步至 dff2b55（`cat >` 原地改写，sha256=
900dcb/c74b79/3686be/…，**下次重启后生效源**）；②当机会话以 **per-file bind mount**
（`/data/local/tmp/live-dff2b55/*` 0755，`adb push` Windows 源无执行位曾致 0666
 Permission denied 一过性事故，umount 复原后重推修复）将四文件叠上 /system/bin，
nsenter 验证 init/daemon/adbd 三视图一致可见。converge 冷启动后加载
`Runtime library loaded (v1.32.0)`=修后码；孪生围栏实验（9.3-D-02）反证运行时已换。
**bind 与 staging 目录须存活至下次重启**（重启后由已升级模块目录无缝接管，内容同源
dff2b55；staging 若先删则当机会话回退 pre-fix——收尾清单标注）。

### 9.3 延后三项修后真机复验（全部达成，新证据）

trace：`tests/results/device-192.168.2.156:36777-20260910-171546-phaseD.log`（+
`…-173500-phaseD-recheck.log`）。

| 项 | 场景构造 | 结果与关键证据 |
| :-- | :-- | :-- |
| **D-P6-10-02** 锁主围栏 | 删锁+起孪生（run#18 后证形：原持锁者变非持有者） | **5/5 PASS**：20s 内收敛单实例；围栏审计行原文 `[2026-09-10 17:16:05] [WARNING] 🔒 Lock-owner fence: live daemon (PID 20077) holds the lock; non-holder (PID 24552) exiting (single-resident invariant, D-P6-10-02)`；原 PID 干净自退、新持锁者唯一存活、Alive；静置 210s+190s 双窗审计 **每 token op=tick 恒 1 行**（pre-fix 病征为 2–3 行/分钟） |
| **D-P6-10-01** 扫描年龄钳制 | runbook 清理后 cycle-* 已空 → 按 P6-11 §7-A2 以**套件外构造**复现旧饿死场景：9 枚链根 + 25h 前 token 的单 cycle 文件 + 新链 root@+2min，managed 冷启动 | **5/5 PASS**：9 枚陈旧根**零重登记**（dag/ 下 p6_old* 目录=0）、**零入 .scanpending**、窗口审计 `action=limit` 总数=0（pre-fix 病征=8 僵尸占满名额→limit 拒新）；新链 120s 登记、run 收敛 SUCCESS——陈旧残留与新链共存不饿死 |
| **D-P6-10-03** 终局拒重放 | G4 微窗真机口径：rc→cf(exit1, retry.max=1, interval=0) 链，2s 粒度监听 attempt-2 落地瞬间 kill -9 持锁 daemon（root@1725→attempt-2 检出 280s→即杀） | **6/6 PASS**：watchdog 拉起 ✓；run 终局 FAILED ✓；节点执行 **2 次（=1+max 钳制；run#16 pre-fix 病征=终局后第 3 跑）**；`retry.count=1=max`、`retry.until` 已清（arm 恒拒）；终局后 +180s 静默窗执行计数 2→2 **零重放** |

Phase D 首跑 15 PASS/2 FAIL，两处 FAIL 均为**取证 helper 自身引号 bug**
（设备命令内嵌单引号经 su-c 外层拼接后管道被设备外层 shell 劫持）→ 宿主侧管线
复测转绿（trace 同上），产品判据零 FAIL。

### 9.4 套件复跑与组合结果

| 项 | 结果 | 判定 |
| :-- | :-- | :-- |
| p6-device 修后复跑（**纯时序加固版首跑**，16:25–17:10，load1=12.5 起测+180s 沉降） | **PASS=56 FAIL=0 SKIP=0**（2676s；含波 B 全绿、1 条 [note] pre-settle 超时按扩大窗进行） | ✅ run#18 后加固判据真机成立 |
| p1-device 独立（17:4x） | **11/0/2** 保持 | ✅ |
| p3-device 独立（17:5x–18:1x） | **28/0/0/BLOCKED=0** 保持 | ✅ |
| `run_tests.sh --with-device` 组合（18:09 起；宿主 51 套件全绿至 build_check 29/0） | 设备段三败：**p1 8/3/2、p3 suite TIMEOUT@900s、p6 45/11**——全程处 **用户 App 负载风暴**（loadavg 9.7→15→20→55 实测带，tick 掉洞 18:25-27/1839-43 等，IPC operation_timeout×5 重试耗尽；10-restore 期网络 adb **再度失联**，空读致末段 FAIL） | ❌ 未达组合全绿 = **环境项**（P6-10 §5.3 同款病征：同批断言 40 分钟前分项全绿；宿主段全绿）→ 按预案标注 **组合全绿待下一次窗口** |
| 组合后链路 | 20:2x 起 `adb offline`、ping 不通（风暴后整机休眠/网络栈掉电形态，P6-10 04:37 先例复现）；恢复后需先跑 §7 runbook 增量清理（本轮组合遗留：MANAGED + `p6_ra/rb/rc/ya/sb…` HHMM 任务（明日 19:17/19:36 到点复跑风险）+ p3 遗留 `t1_boot/t2_0830/edit1/hproc` + config `0830` 行） | ⚠️ 见 §9.6 遗留 |

### 9.5 现场恢复（截至文档完成时点）

- 模块目录=**dff2b55 构建内容**（重启后生效源）；当机会话=bind 修后码（重启前等价）；
- 独立复跑/Phase D 后设备曾达 **legacy+空 config 基线**（d41d8…，phaseD cleanup PASS）；
  组合跑因风暴+断链未及自洁 → **§9.4 末行残留清单待链路恢复后增量再清**；
- p6-snap 备份组（3 vault + module-dir-pre-dff2b55 + config 快照）与 bind staging
  `/data/local/tmp/live-dff2b55`、审计留证件随残留一并处置（staging 在下次重启前**勿删**）。
- **会话终止时点链路状态（21:13 最后探测）**：`192.168.2.156:36777` offline、ICMP 无
  响应、mDNS 无 adb-tls 服务记录——整机休眠/断网（P6-10 04:37 同款形态），工具侧无法
  唤醒。**下一窗口动作序**（全部为既有 runbook/套件语义）：
  ①`adb connect` 恢复后按 §7 步骤 2 增量清理组合遗留（命令原文见 §9.4 末行清单——
  `p6_*`/`p6_s*` HHMM 任务明日 08:30/19:17/19:36 到点复跑风险，先清再动）；
  ②复核三 bind 与 staging（若设备曾重启：模块目录=同内容修后构建，无缝接管，仅缺
  staging 目录可删）；③`run_tests.sh --with-device` 安静窗复跑（预期组合全绿：
  宿主 51 + p1 11/0/2 + p3 28/0 + p6 56/0 各段均已在今日取得同机同日分项证据）。

### 9.6 新发现登记

本轮复验**无新增产品缺陷**。新增环境/基建事实两条（登记于此，不立产品项）：
① KernelSU×A16（25113PN0EC）模块运行态=boot-time tmpfs 拷贝 overlay，ro、upper 不可见
——文件级热升级须走 per-file bind mount（或 ksud 正规 install+reboot）；
② `adb push` 自 Windows 源不带执行位（0666）→ bind 后 Permission denied 事故面，
纪律=push 后强制 chmod 755 再 mount。
